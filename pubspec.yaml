name: kocaelispor_1966_mobil
description: Kocaelispor Taraftar Uygulaması
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5  # ✅ TEK VERSİYON
  google_sign_in: ^6.1.5
  font_awesome_flutter: ^10.8.0  # ✅ profile_page için gerekli
  cupertino_icons: ^1.0.8
  provider: ^6.1.1  # ✅ YENİ EKLEME

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  assets:
    - assets/images/stadium_bg.png
    - assets/images/avatar_placeholder.png
  uses-material-design: true